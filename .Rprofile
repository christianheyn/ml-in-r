
initSettings <- function () {
    r = getOption("repos")
    r["CRAN"] = "http://cran.uk.r-project.org" # hard code the UK repo for CRAN
    invisible(options(repos = r))
    invisible(rm(r))
}

initSettings()
